{
    // common for both autoClient and echoServer
    "autoSendPeriod": 60000,
    "useSameSessionKeyCount": 1,
    // Only for autoClient
    "connectionTimeout": 50000,
    // for migration of both autoClient and echoServer
    "migrationEnabled": true,
    "authFailureThreshold": 2,
    "migrationFailureThreshold": 2
}
